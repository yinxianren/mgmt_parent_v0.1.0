<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- 注解扫描非Controller注解 -->
    <context:component-scan base-package="com.rxh.anew">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <context:annotation-config/>

    <!-- 全局配置信息对象 -->
    <bean id="globalConfiguration" class="com.rxh.utils.GlobalConfiguration">
        <property name="fileUploadPath" value="${file.upload.path}"/>
        <property name="production" value="${production.flag}"/>
    </bean>

    <!-- RSA加密 -->
    <bean id="rsaUtils" class="com.rxh.anew.tools.RSAUtils">
        <constructor-arg name="publicKeyPath" value="${rsa.key.path.public}"/>
        <constructor-arg name="privateKeyPath" value="${rsa.key.path.private}"/>
        <constructor-arg name="globalConfiguration" ref="globalConfiguration"/>
    </bean>

    <!-- 可通过SpringContextUtil获取Spring容器中的任何bean -->
    <bean id="springContextUtil" class="com.rxh.anew.tools.SpringContextUtil" scope="singleton" />

    <!-- 多语言资源获取 -->
    <bean id="messageSource" class="org.springframework.context.support.ResourceBundleMessageSource">
        <property name="defaultEncoding" value="UTF-8" />
        <property name="useCodeAsDefaultMessage" value="true" />
        <property name="basenames">
            <list>
                <value>i18.pay</value>
                <value>i18.error</value>
            </list>
        </property>
    </bean>

    <!-- 对@Aspectj注解支持 -->
    <aop:aspectj-autoproxy proxy-target-class="true"/>

</beans>