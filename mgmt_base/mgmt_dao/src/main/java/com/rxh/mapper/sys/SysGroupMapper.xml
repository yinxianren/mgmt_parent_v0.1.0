<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rxh.mapper.sys.SysGroupMapper">

  <resultMap id="BaseResultMap" type="com.rxh.pojo.sys.SysGroup">
    <id column="code" jdbcType="VARCHAR" property="code" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="model" jdbcType="SMALLINT" property="model" />
    <result column="system" jdbcType="SMALLINT" property="system" />
  </resultMap>
  <sql id="Base_Column_List">
    code, name, model, system
  </sql>
  
  <!--动态条件 -->
  <sql id="where_filters">
		<where>
			<if test="name !=null and name !=''">
				and name=#{name}
			</if>
			<if test="code !=null and code !=''">
				and code=#{code}
			</if>
			<if test="model !=null and model !=''">
				and model=#{model}
			</if>
			<if test="system !=null and system !=''">
				and system=#{system}
			</if>
		</where>
	</sql>
  <sql id="where_filters2">
		<where>
          <if test="name !=null and name !=''">
            and name like CONCAT('%',#{name},'%')
          </if>
          <if test="code !=null and code !=''">
            and code like CONCAT('%',#{code},'%')
          </if>
          <if test="model !=null and model !=''">
            and model=#{model}
          </if>
          <if test="system !=null and system !=''">
            and system=#{system}
          </if>

		</where>
	</sql>



    <sql id="Sys_group_Info_List">
      sg.name,sg,code,sg.model,sg.system
    </sql>
	
  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from sys_group
    where code = #{code,jdbcType=VARCHAR}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    delete from sys_group
    where code = #{code,jdbcType=VARCHAR}
  </delete>
  <insert id="insert" parameterType="com.rxh.pojo.sys.SysGroup">
    insert into sys_group (code, name, model, 
      system)
    values (#{code,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}, #{model,jdbcType=SMALLINT}, 
      #{system,jdbcType=SMALLINT})
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.rxh.pojo.sys.SysGroup">
    update sys_group
    <set>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="model != null">
        model = #{model,jdbcType=SMALLINT},
      </if>
      <if test="system != null">
        system = #{system,jdbcType=SMALLINT},
      </if>
    </set>
    where code = #{code,jdbcType=VARCHAR}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.rxh.pojo.sys.SysGroup">
    update sys_group
    set name = #{name,jdbcType=VARCHAR},
      model = #{model,jdbcType=SMALLINT},
      system = #{system,jdbcType=SMALLINT}
    where code = #{code,jdbcType=VARCHAR}
  </update>
  
  	<!--根据条件查询列表 -->
	<select id="queryList" parameterType="map" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from sys_group
		<include refid="where_filters2" />
        <if test="pageFirst!=null">
        limit #{pageFirst},#{pageSize}
       </if>

	</select>
    <select id="selectAll" resultType="com.rxh.pojo.sys.SysGroup">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_group
    </select>



    <!--根据条件查询列表 -->
    <select id="selectOrderBySearchInfo" parameterType="map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_group
        <include refid="where_filters"/>

    </select>




  <insert id="insertSelective" parameterType="com.rxh.pojo.sys.SysGroup">
    insert into sys_group
    <trim prefix="(" suffix=")" suffixOverrides=",">

      <if test="name != null">
        name,
      </if>
      <if test="code != null">
        code,
      </if>
      <if test="model != null">
        model,
      </if>
      <if test="system!= null">
        system,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">

      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="code != null">
        #{code,jdbcType=VARCHAR},
      </if>
      <if test="model != null">
        #{model,jdbcType=SMALLINT},
      </if>
      <if test="system != null">
        #{system,jdbcType=SMALLINT},
      </if>
    </trim>
  </insert>

  <!--根据条件查询列表 -->
  <select id="getConstantAllResultCount" parameterType="map" resultType="int">
    select
    count(1)
    from sys_group
    <include refid="where_filters"/>
  </select>

  <select id="selectUserByGroupName" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM sys_group
    WHERE name = #{groupName}
  </select>
  <select id="selectUserByGroupCode" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM sys_group
    WHERE code = #{groupCode}
  </select>

</mapper>